import { PorcentajePipe } from './porcentaje.pipe';

describe('PorcentajePipe', () => {
  it('create an instance', () => {
    const pipe = new PorcentajePipe();
    expect(pipe).toBeTruthy();
  });
});
